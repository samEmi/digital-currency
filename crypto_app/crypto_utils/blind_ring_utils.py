def trapgen(n):
    return A, B_a

def setup(n):
    ''' PPT algorithm which takes as input security param n and outputs a set of public params P.'''
    #P = (n, m, q, k, κ, σ, H, σ1, σ2, σ3, M1, M2, M3, T, η)
    
    pass

def key_gen(P):
    '''PPT algorithm which takes as input public params P and a (pk=A, sk=S) pair.'''
    A, B_a = trapgen(n)
    return (A, S)